class HTMLPeeriPeeriHomeElement extends HTMLElement {
    static observedAttributes = ['theme', 'rows', 'columns']
    constructor() {
        super()

        this.attributeChangeCallbacks = Map()
    }

    connectedCallback() {}

    attributeChangedCallback(name, oldValue, newValue) {
        if (oldValue != newValue)
            switch (name) {
                case 'theme':
                    // this.open(newValue);
                    break;
                case 'rows':
                    // this.open(newValue);
                    break;
                case 'columns':
                    // this.open(newValue);
                    break;
            }
    }
} customElements.define('img-', HTMLPeeriPeeriHomeElement)
